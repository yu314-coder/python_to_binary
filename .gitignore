__pycache__/
*.py[cod]
.DS_Store
.pytest_cache/
.py2bin-cache/
build/
dist/
*.egg-info/

# local build/test scratch on disk D
.py2bin-work/
dist_release/

# Everything the assistant keeps here: workflows, agent definitions, and the
# worktrees a background workflow makes (each is a copy of this repo). None of
# it is part of what py2bin is, and it does not belong to anyone who clones.
.claude/
